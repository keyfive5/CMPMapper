services:
  - type: web
    name: cmp-mapper
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python cmp_mapper_pro.py
    envVars:
      - key: PORT
        value: 10000
